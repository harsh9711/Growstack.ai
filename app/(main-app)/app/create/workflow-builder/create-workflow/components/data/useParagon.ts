import { useCallback, useEffect, useState } from "react";
import paragon, { SDK_EVENT } from "@useparagon/connect"; // Ensure correct import


// await paragon.authenticate(projectId, paragonUserToken);